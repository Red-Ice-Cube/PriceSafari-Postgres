@model IEnumerable<StoreClass>

@{
    ViewBag.Title = "Wybierz Sklep";
}



@{
    Layout = "~/Views/Shared/_PanelLayout.cshtml";
}

<div class="Vert">
    <div class="Vert-full">
        <div class="Order-Container-Title">
            Konkurencja
        </div>
        @if (Model != null && Model.Any())
        {
            <div class="Vert-table">
                <table class="table-orders">
                    <thead>
                        <tr>
                            <th></th>
                            <th>Sklep</th>
                          
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var store in Model)
                        {
                            <tr class="store-row" style="cursor:pointer;" data-url="@Url.Action("Competitors", "Competitors", new { storeId = store.StoreId })">
                                <td>
                                    <img src="@store.StoreLogoUrl" class="img-container" alt="@store.StoreName" />
                                </td>
                                <td>@store.StoreName</td>
                                
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        }
        else
        {
            <p>Nie masz przypisanych żadnych sklepów.</p>
        }
    </div>
</div>

@section Scripts {
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            var rows = document.querySelectorAll('.store-row');
            rows.forEach(function (row) {
                row.addEventListener('click', function (e) {
                    window.location.href = row.getAttribute('data-url');
                });
            });
        });
    </script>
}
