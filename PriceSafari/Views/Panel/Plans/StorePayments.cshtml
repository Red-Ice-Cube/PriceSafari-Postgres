@using Newtonsoft.Json
@using Newtonsoft.Json.Serialization
@model PriceSafari.Models.ViewModels.StorePaymentsViewModel

@{
    Layout = "~/Views/Shared/_PanelLayout.cshtml";
    var jsonSettings = new JsonSerializerSettings
    {
        ContractResolver = new CamelCasePropertyNamesContractResolver()
    };
    var paymentDataListJson = JsonConvert.SerializeObject(Model.PaymentDataList, jsonSettings);

    string planImageFile = "";
    if (Model.IsTestPlan) planImageFile = "Free.png";
    else if (Model.ProductsToScrap <= 500) planImageFile = "Lite.png";
    else if (Model.ProductsToScrap <= 1000) planImageFile = "Basic.png";
    else if (Model.ProductsToScrap <= 4000) planImageFile = "Pro.png";
    else if (Model.ProductsToScrap <= 10000) planImageFile = "Ultimate.png";
    else planImageFile = "Enterprise.png";

    // Obliczenia ceny do użycia w modalu i widoku głównym
    decimal discountedPrice = Model.PlanPrice;
    if (Model.DiscountValue.HasValue && Model.DiscountValue.Value > 0)
    {
        var discountAmount = Model.PlanPrice * (Model.DiscountValue.Value / 100);
        discountedPrice = Model.PlanPrice - discountAmount;
    }
}

<style>
    .details-container {
        display: flex;
        gap: 30px;
        width: 100%;
        margin-top: 24px;
        align-items: stretch;
    }

    .details-main {
        flex: 3;
    }

    .details-sidebar {
        flex: 2;
    }

    .info-card {
        border: 1px solid #e9ecef;
        border-radius: 8px;
        background-color: #fff;
        display: flex;
        flex-direction: column;
    }

    .info-card-header {
        padding: 16px 20px;
        background-color: #f8f9fa;
        border-bottom: 1px solid #e9ecef;
        font-size: 17px;
        font-weight: 500;
    }

    .info-card-body {
        padding: 20px;
        flex-grow: 1;
    }

    .info-card-footer {
        padding: 20px;
        background-color: #f8f9fa;
        border-top: 1px solid #e9ecef;
    }

    .info-row {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 14px 0;
        border-bottom: 1px solid #f1f3f5;
    }

        .info-row:last-child {
            border-bottom: none;
            padding-bottom: 0;
        }

        .info-row:first-child {
            padding-top: 0;
        }

    .info-label {
        color: #6c757d;
    }

    .info-value {
        color: #212529;
        font-weight: 500;
        text-align: right;
    }

        .info-value img {
            height: 20px;
            width: 20px;
            margin-left: 5px;
        }

    .price-display {
        display: flex;
        align-items: baseline;
        margin-bottom: 8px;
    }

        .price-display .final-price-amount {
            color: #212529;
            font-weight: 600;
            font-size: 28px;
            line-height: 1;
        }

        .price-display .final-price-label {
            font-size: 16px;
            font-weight: 400;
            color: #6c757d;
            margin-left: 8px;
        }

        .price-display .original-price {
            text-decoration: line-through;
            font-size: 18px;
            color: #6c757d;
            margin-left: 12px;
        }

    .discount-info {
        font-weight: 500;
        color: #28a745;
    }

    /* Modal styles */
    .modal-dialog {
        min-width: 1000px; 
    }

    .modal-content {
        min-width: 1000px; 
    }


    .modal-split {
        display: flex;
        gap: 24px;
        padding: 1rem;
    }

    .modal-split-left {
        flex: 5;
    }

    .modal-split-right {
        flex: 4;
    }

    .space-100 {
        display: flex;
        justify-content: space-between;
        width: 100%;
        margin-top: 16px;
        margin-bottom: 24px;
    }

    .custom-info-table {
        border-collapse: separate;
        border-spacing: 0;
        width: 100%;
        border: 1px solid #e9ecef;
        border-radius: 8px;
        overflow: hidden;
    }

        .custom-info-table th,
        .custom-info-table td {
            padding: 10px 14px;
            border-bottom: 1px solid #e9ecef;
        }

        .custom-info-table th {
            background-color: #f8f9fa;
            text-align: left;
            font-weight: normal;
            color: #6c757d;
            width: 120px;
        }

        .custom-info-table td {
            font-weight: 500;
        }

        .custom-info-table tr:last-child td,
        .custom-info-table tr:last-child th {
            border-bottom: none;
        }
</style>

<div class="Vert">
    <div class="Vert-full">
        <div class="store-details">
            <h2 style="font-size: 26px; font-weight: 300;">Plan @Model.PlanName | <img src="~/images/@planImageFile" alt="@Model.PlanName" style="height:32px; margin-bottom:6px;" /></h2>

            <div class="details-container">

                <div class="details-sidebar info-card">
                    <div class="info-card-header">
                        Koszt planu
                    </div>
                    <div class="info-card-body">
                        <div class="price-display">
                            <span class="final-price-amount">@discountedPrice.ToString("C")</span>
                            <span class="final-price-label">netto</span>
                            @if (Model.DiscountValue.HasValue && Model.DiscountValue.Value > 0)
                            {
                                <span class="original-price">@Model.PlanPrice.ToString("C")</span>
                            }
                        </div>
                        @if (Model.DiscountValue.HasValue && Model.DiscountValue.Value > 0)
                        {
                            <div class="discount-info">
                                Rabat: @Model.DiscountValue.Value.ToString("F2")%
                            </div>
                        }

                        @if (!string.IsNullOrWhiteSpace(Model.Info))
                        {
                            <hr style="margin: 20px 0;">
                            <div class="info-description">
                                <p style="color: #6c757d; font-size: 0.9em; margin-bottom: 0;">@Model.Info</p>
                            </div>
                        }
                    </div>
                    <div class="info-card-footer">
                        @if (Model.IsTestPlan)
                        {
                            <p class="mb-0"><strong>Generowanie faktur niedostępne w planie testowym.</strong></p>
                        }
                        else if (Model.HasUnpaidInvoice)
                        {
                            <p class="mb-0"><strong>Proforma została już wygenerowana.</strong> Sprawdź w historii poniżej.</p>
                        }
                        else
                        {
                            <button type="button" class="Button-Page-Small-bl" data-toggle="modal" data-target="#generateProformaModal">
                                Wygeneruj Proformę
                            </button>
                        }
                    </div>
                </div>
                <div class="details-main info-card">
                    <div class="info-card-header">
                        Szczegóły planu
                    </div>
                    <div class="info-card-body">
                        <div class="info-row">
                            <span class="info-label">Źródło danych</span>
                            <span class="info-value">
                                @if (Model.Ceneo)
                                {
                                    <img src="~/images/Ceneo.png" alt="Ceneo" title="Ceneo" />
                                }
                                @if (Model.GoogleShopping)
                                {
                                    <img src="~/images/GoogleShopping.png" alt="Google Shopping" title="Google Shopping" />
                                }
                                @if (Model.Allegro)
                                {
                                    <img src="~/images/AllegroIcon.png" alt="Allegro" title="Allegro" />
                                }
                            </span>
                        </div>
                        <div class="info-row">
                            <span class="info-label">Limity produktów</span>
                            <span class="info-value">
                                <div>Do <b>@Model.ProductsToScrap</b> SKU w porównywarkach</div>
                                @if (Model.Allegro)
                                {
                                    <div class="mt-1">Do <b>@Model.ProductsToScrapAllegro</b> SKU na marketplace</div>
                                }
                            </span>
                        </div>
                        <div class="info-row">
                            <span class="info-label">Pakiet analiz</span>
                            <span class="info-value">@Model.ScrapesPerInvoice aktualizacji danych</span>
                        </div>
                        <div class="info-row">
                            <span class="info-label">Dane historyczne</span>
                            <span class="info-value">30 analiz historycznych</span>
                        </div>
                    </div>
                </div>

            </div>
        </div>

        <div class="modal fade" id="generateProformaModal" tabindex="-1" role="dialog" aria-labelledby="generateProformaModalLabel" aria-hidden="true">
            <div class="modal-dialog" role="document">
                <div class="modal-content">
                    <div class="modal-header">
                        <h5 class="modal-title" id="generateProformaModalLabel">Generowanie Proformy</h5>
                        <button type="button" class="close" data-dismiss="modal" aria-label="Zamknij">
                            <span aria-hidden="true">&times;</span>
                        </button>
                    </div>
                    <div class="modal-body">
                        <div class="modal-split">
                            <div class="modal-split-left">
                                <div class="form-group">
                                    <select id="paymentDataSelect" class="form-control" style="margin-top:0px;">
                                        <option value="">Wybierz istniejące dane do faktury</option>
                                        @foreach (var data in Model.PaymentDataList)
                                        {
                                            <option value="@data.PaymentDataId">@data.CompanyName</option>
                                        }
                                    </select>
                                    <div class="space-100">
                                        <div>
                                            <button type="button" class="Button-Page-Small-bl" id="addPaymentDataButton">+ Nowe dane</button>
                                        </div>
                                        <div>
                                            <button type="button" class="Button-Page-Small" id="editPaymentDataButton" style="display:none;">Edytuj</button>
                                            <button type="button" class="Button-Page-Small-r" id="deletePaymentDataButton" style="display:none;">Usuń</button>
                                        </div>
                                    </div>
                                </div>

                                <div id="paymentDataDetails" style="display:none;">
                                    <table class="custom-info-table">
                                        <tr>
                                            <th>Nazwa firmy</th>
                                            <td id="companyName"></td>
                                        </tr>
                                        <tr>
                                            <th>Adres</th>
                                            <td id="address"></td>
                                        </tr>
                                        <tr>
                                            <th>Kod pocztowy</th>
                                            <td id="postalCode"></td>
                                        </tr>
                                        <tr>
                                            <th>Miasto</th>
                                            <td id="city"></td>
                                        </tr>
                                        <tr>
                                            <th>NIP</th>
                                            <td id="nip"></td>
                                        </tr>
                                    </table>
                                </div>

                                <div id="paymentDataFormContainer" style="display:none;">
                                    <form id="paymentDataForm">
                                        <input type="hidden" id="PaymentDataId" name="PaymentDataId" value="" />
                                        <div class="form-group">
                                            <label for="CompanyName">Nazwa firmy</label>
                                            <input type="text" class="form-control" id="CompanyName" name="CompanyName" required />
                                        </div>
                                        <div class="form-group">
                                            <label for="Address">Adres</label>
                                            <input type="text" class="form-control" id="Address" name="Address" required />
                                        </div>
                                        <div class="form-group">
                                            <label for="PostalCode">Kod pocztowy</label>
                                            <input type="text" class="form-control" id="PostalCode" name="PostalCode" required />
                                        </div>
                                        <div class="form-group">
                                            <label for="City">Miasto</label>
                                            <input type="text" class="form-control" id="City" name="City" required />
                                        </div>
                                        <div class="form-group">
                                            <label for="NIP">NIP</label>
                                            <input type="text" class="form-control" id="NIP" name="NIP" required />
                                        </div>
                                        <div class="form-group">
                                            <label for="InvoiceAutoMail">E-mail do automatycznej wysyłki faktur (opcjonalny)</label>
                                            <input type="email" class="form-control" id="InvoiceAutoMail" name="InvoiceAutoMail" />
                                        </div>
                                        <div class="form-group form-check">
                                            <input type="checkbox" class="form-check-input" id="InvoiceAutoMailSend" name="InvoiceAutoMailSend" value="true" />
                                            <label class="form-check-label" for="InvoiceAutoMailSend">Automatyczna wysyłka faktur na e-mail</label>
                                        </div>
                                        <div style="margin-top:16px;">
                                            <button type="button" class="Button-Page-Small-bl" id="savePaymentDataButton">Zapisz dane</button>
                                            <button type="button" class="Button-Page-Small" id="cancelPaymentDataButton">Anuluj</button>
                                        </div>
                                    </form>
                                </div>
                            </div>
                            <div class="modal-split-right">
                                <div class="info-card" style="height: 100%;">
                                    <div class="info-card-header">
                                        Podsumowanie planu
                                    </div>
                                    <div class="info-card-body">
                                        <p style="font-weight: 500; font-size: 1.1em; margin-bottom: 14px;">
                                           
                                            Plan @Model.PlanName
                                        </p>

                                        <div class="price-display">
                                            <span class="final-price-amount">@discountedPrice.ToString("C")</span>
                                            <span class="final-price-label">netto</span>
                                            @if (Model.DiscountValue.HasValue && Model.DiscountValue.Value > 0)
                                            {
                                                <span class="original-price">@Model.PlanPrice.ToString("C")</span>
                                            }
                                        </div>
                                        @if (Model.DiscountValue.HasValue && Model.DiscountValue.Value > 0)
                                        {
                                            <div class="discount-info" style="margin-bottom: 20px;">
                                                Rabat: @Model.DiscountValue.Value.ToString("F2")%
                                            </div>
                                        }

                                        
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-secondary" data-dismiss="modal">Anuluj</button>
                        <form id="generateProformaForm" method="post" asp-action="GenerateProforma" style="margin:0;">
                            <input type="hidden" name="storeId" value="@Model.StoreId" />
                            <input type="hidden" id="paymentDataId" name="paymentDataId" value="" />
                            <button type="submit" class="btn btn-success" id="generateProformaButton" disabled>Generuj i pobierz Proformę</button>
                        </form>
                    </div>
                </div>
            </div>
        </div>

        @if (Model.Invoices != null && Model.Invoices.Any())
        {
            <div class="Vert-table" style="margin-top: 30px;">
                <h3 style="font-weight: 400; margin-bottom: 20px;">Historia płatności</h3>
                <table class="table-orders">
                    <thead>
                        <tr>
                            <th>Numer</th>
                            <th>Wystawiono</th>
                            <th>Opłacono</th>
                            <th>Kwota Netto</th>
                            <th>Status</th>
                            <th>Pobierz</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var invoice in Model.Invoices)
                        {
                            <tr class="store-row">
                                <td>@invoice.InvoiceNumber</td>
                                <td>@invoice.IssueDate.ToString("yyyy-MM-dd")</td>
                                <td>@(invoice.PaymentDate?.ToString("yyyy-MM-dd") ?? "N/A")</td>
                                <td>
                                    @if (invoice.AppliedDiscountPercentage > 0)
                                    {
                                        var originalPrice = invoice.NetAmount + invoice.AppliedDiscountAmount;
                                        <div style="display:flex; flex-direction:column; align-items:flex-start;">
                                            <div>
                                                <span style="color: darkgreen; font-weight: bold;">@invoice.NetAmount.ToString("C")</span>
                                                <span style="text-decoration: line-through; font-weight: normal; color: #222222; margin-left: 10px;">@originalPrice.ToString("C")</span>
                                            </div>
                                            <div style="font-size:0.9em; color: #222222; margin-top: 4px;">
                                                Rabat: @invoice.AppliedDiscountPercentage.ToString("F2")% (@invoice.AppliedDiscountAmount.ToString("C"))
                                            </div>
                                        </div>
                                    }
                                    else
                                    {
                                        <span>@invoice.NetAmount.ToString("C")</span>
                                    }
                                </td>
                                <td>
                                    @if (invoice.IsPaid)
                                    {
                                        <span class="Validation Validation-accepted">Opłacona</span>
                                    }
                                    else
                                    {
                                        <span class="Validation Validation-cancelled">Nieopłacona</span>
                                    }
                                </td>
                                <td>
                                    <a href="@Url.Action("InvoicePdf", "Payment", new { invoiceId = invoice.InvoiceId })" class="Button-Action-Small" target="_blank">
                                        Pobierz
                                    </a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        }
        else
        {
            <p style="margin-top: 40px;">Brak historii faktur dla tego sklepu.</p>
        }
    </div>
</div>

@section Scripts {
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.bundle.min.js"></script>
    <script>
        $(document).ready(function () {
            var paymentDataList = @Html.Raw(paymentDataListJson);

            function resetForm() {
                $("#paymentDataForm")[0].reset();
                toggleAutoMailSendCheckbox("");
                $("#PaymentDataId").val('');
            }

            function showForm() {
                $("#paymentDataFormContainer").show();
                $("#paymentDataDetails").hide();
                $("#generateProformaButton").prop('disabled', true);
            }

            function hideForm() {
                $("#paymentDataFormContainer").hide();
            }

            function showDetails() {
                $("#paymentDataDetails").show();
                $("#editPaymentDataButton").show();
                $("#deletePaymentDataButton").show();
            }

            function hideDetails() {
                $("#paymentDataDetails").hide();
                $("#editPaymentDataButton").hide();
                $("#deletePaymentDataButton").hide();
            }

            function clearDetailsFields() {
                $("#companyName").text('');
                $("#address").text('');
                $("#postalCode").text('');
                $("#city").text('');
                $("#nip").text('');
            }

            function disableGenerateProforma() {
                $("#generateProformaButton").prop('disabled', true);
            }

            function enableGenerateProforma() {
                $("#generateProformaButton").prop('disabled', false);
            }

            function toggleAutoMailSendCheckbox(emailValue) {
                if (!emailValue || emailValue.trim() === "") {
                    $("#InvoiceAutoMailSend").prop("checked", false);
                    $("#InvoiceAutoMailSend").prop("disabled", true);
                } else {
                    $("#InvoiceAutoMailSend").prop("disabled", false);
                }
            }

            function populatePaymentData(paymentDataId) {
                var data = paymentDataList.find(x => x.paymentDataId == paymentDataId);
                if (data) {
                    $("#companyName").text(data.companyName);
                    $("#address").text(data.address);
                    $("#postalCode").text(data.postalCode);
                    $("#city").text(data.city);
                    $("#nip").text(data.nip);
                    showDetails();
                    hideForm();
                    enableGenerateProforma();
                    $("#paymentDataId").val(data.paymentDataId);
                } else {
                    hideDetails();
                    clearDetailsFields();
                    $("#paymentDataId").val('');
                    disableGenerateProforma();
                }
            }

            $("#paymentDataSelect").change(function () {
                var selectedValue = $(this).val();
                if (selectedValue) {
                    populatePaymentData(selectedValue);
                } else {
                    hideDetails();
                    clearDetailsFields();
                    $("#paymentDataId").val('');
                    disableGenerateProforma();
                }
            });

            $("#addPaymentDataButton").click(function () {
                resetForm();
                showForm();
                hideDetails();
                disableGenerateProforma();
            });

            $("#editPaymentDataButton").click(function () {
                var paymentDataId = $("#paymentDataId").val();
                var data = paymentDataList.find(x => x.paymentDataId == paymentDataId);
                if (data) {
                    showForm();
                    hideDetails();
                    disableGenerateProforma();
                    $("#PaymentDataId").val(data.paymentDataId);
                    $("#CompanyName").val(data.companyName);
                    $("#Address").val(data.address);
                    $("#PostalCode").val(data.postalCode);
                    $("#City").val(data.city);
                    $("#NIP").val(data.nip);
                    $("#InvoiceAutoMail").val(data.invoiceAutoMail || '');
                    $("#InvoiceAutoMailSend").prop('checked', data.invoiceAutoMailSend);
                    toggleAutoMailSendCheckbox($("#InvoiceAutoMail").val());
                } else {
                    alert("Nie można edytować - dane nie istnieją.");
                }
            });

            $("#cancelPaymentDataButton").click(function () {
                hideForm();
                var paymentDataId = $("#paymentDataId").val();
                if (paymentDataId && $("#paymentDataSelect").val()) { // Only show details if an item is selected
                    populatePaymentData(paymentDataId);
                } else {
                    hideDetails();
                    clearDetailsFields();
                    disableGenerateProforma();
                }
            });

            $("#InvoiceAutoMail").on("input", function () {
                toggleAutoMailSendCheckbox($(this).val());
            });

            $("#savePaymentDataButton").click(function () {
                var formData = $("#paymentDataForm").serialize();
                $.ajax({
                    type: "POST",
                    url: "@Url.Action("SaveOrUpdatePaymentData", "Payment")",
                    data: formData,
                    success: function (response) {
                        if (response.success) {
                            var newPaymentData = response.paymentData;
                            var existingData = paymentDataList.find(x => x.paymentDataId == newPaymentData.paymentDataId);
                            if (existingData) {
                                Object.assign(existingData, newPaymentData);
                                $("#paymentDataSelect option[value='" + newPaymentData.paymentDataId + "']").text(newPaymentData.companyName);
                            } else {
                                paymentDataList.push(newPaymentData);
                                $("#paymentDataSelect").append($('<option>', {
                                    value: newPaymentData.paymentDataId,
                                    text: newPaymentData.companyName
                                }));
                            }
                            $("#paymentDataSelect").val(newPaymentData.paymentDataId);
                            hideForm();
                            populatePaymentData(newPaymentData.paymentDataId);
                        } else {
                            alert("Błąd podczas zapisywania danych: Niepoprawne dane wejściowe.");
                        }
                    },
                    error: function (xhr) {
                        if (xhr.responseJSON && xhr.responseJSON.errors) {
                            var errors = xhr.responseJSON.errors;
                            var errorMessage = "Błędy walidacji:\n";
                            for (var key in errors) {
                                errorMessage += errors[key].join(", ") + "\n";
                            }
                            alert(errorMessage);
                        } else {
                            alert("Błąd podczas zapisywania danych (HTTP " + xhr.status + ").");
                        }
                    }
                });
            });

            $("#deletePaymentDataButton").click(function () {
                if (confirm("Czy na pewno chcesz usunąć te dane?")) {
                    var paymentDataId = $("#paymentDataId").val();
                    if (!paymentDataId) {
                        alert("Brak wybranego adresu do usunięcia.");
                        return;
                    }
                    $.ajax({
                        type: "POST",
                        url: "@Url.Action("DeletePaymentData", "Payment")",
                        data: { paymentDataId: paymentDataId },
                        success: function (response) {
                            if (response.success) {
                                paymentDataList = paymentDataList.filter(x => x.paymentDataId != paymentDataId);
                                $("#paymentDataSelect option[value='" + paymentDataId + "']").remove();
                                $("#paymentDataSelect").val('');
                                hideDetails();
                                clearDetailsFields();
                                disableGenerateProforma();
                                hideForm();
                            } else {
                                alert("Błąd podczas usuwania danych - operacja nieudana po stronie serwera.");
                            }
                        },
                        error: function (xhr) {
                            if (xhr.status == 404) {
                                alert("Nie znaleziono danych do usunięcia (404). Prawdopodobnie zostały już wcześniej usunięte.");
                            } else {
                                alert("Błąd podczas usuwania danych (HTTP " + xhr.status + ").");
                            }
                        }
                    });
                }
            });
        });
    </script>
}